// Production default API (Render). config.json / GRAPHQL_URL on Vercel can override via main.ts + inject script.
export const environment = {
  production: true,
  graphqlUrl: 'https://employee-api-qo00.onrender.com/graphql',
};
